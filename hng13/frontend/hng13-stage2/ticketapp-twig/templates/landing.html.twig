{% extends 'base.html.twig' %}

{% block body %}
<div class="min-h-screen flex flex-col">
  {# Navbar #}
  <nav class="bg-white shadow-md py-4 px-6 flex justify-between items-center site-container">
    <a href="{{ path('app_home') }}" class="text-2xl font-bold text-blue-600">TicketFlow</a>
    <div class="flex gap-4">
      <a href="{{ path('app_login') }}" class="text-blue-600 hover:underline">Login</a>
      <a href="{{ path('app_register') }}" class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700">Get Started</a>
    </div>
  </nav>

  {# Hero Section with SVG wave #}
  <section class="relative flex-1 flex flex-col items-center justify-center text-center bg-gradient-to-b from-blue-50 to-white overflow-hidden">
    <svg class="absolute bottom-0 left-0 w-full" viewBox="0 0 1440 320" xmlns="http://www.w3.org/2000/svg"><path fill="#3b82f6" fill-opacity="0.18" d="M0,160L80,170.7C160,181,320,203,480,186.7C640,171,800,117,960,106.7C1120,96,1280,128,1360,144L1440,160L1440,320L1360,320C1280,320,1120,320,960,320C800,320,640,320,480,320C320,320,160,320,80,320L0,320Z"></path></svg>

    <div class="relative z-10 px-4 max-w-3xl">
      <h1 class="text-4xl sm:text-5xl font-extrabold text-gray-900 mb-4">
        Manage Your <span class="text-blue-600">Event Tickets</span> Seamlessly
      </h1>
      <p class="text-gray-600 text-lg sm:text-xl mb-6">
        Simplify ticket management, monitor progress, and optimize your events effortlessly.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="{{ path('app_register') }}" class="bg-blue-600 hover:bg-blue-700 text-white px-6 py-3 rounded-full text-lg shadow-lg">Get Started</a>
        <a href="{{ path('app_login') }}" class="border border-blue-600 text-blue-600 px-6 py-3 rounded-full text-lg hover:bg-blue-50">Login</a>
      </div>
    </div>

    {# Decorative Circles #}
    <div class="absolute top-10 left-10 w-32 h-32 bg-blue-200 rounded-full blur-3xl opacity-40"></div>
    <div class="absolute bottom-20 right-10 w-40 h-40 bg-indigo-300 rounded-full blur-2xl opacity-50"></div>
  </section>

  {# Features Section #}
  <section class="relative py-16 px-6 bg-white site-container grid grid-cols-1 md:grid-cols-3 gap-8">
    <div class="bg-gray-50 rounded-2xl p-6 shadow hover:shadow-lg transition">
      <h3 class="text-xl font-semibold text-gray-800 mb-2">Create Tickets</h3>
      <p class="text-gray-600">Easily create event tickets with detailed tracking and status management.</p>
    </div>
    <div class="bg-gray-50 rounded-2xl p-6 shadow hover:shadow-lg transition">
      <h3 class="text-xl font-semibold text-gray-800 mb-2">Monitor Progress</h3>
      <p class="text-gray-600">View open, in-progress, or closed tickets in a clear, card-style display.</p>
    </div>
    <div class="bg-gray-50 rounded-2xl p-6 shadow hover:shadow-lg transition">
      <h3 class="text-xl font-semibold text-gray-800 mb-2">Analytics Dashboard</h3>
      <p class="text-gray-600">Gain insights into total and resolved tickets to enhance event performance.</p>
    </div>
  </section>

  {# Footer #}
  <footer class="bg-gray-900 text-gray-300 py-8">
    <div class="site-container text-center">
      <h2 class="text-white text-2xl font-bold mb-4">TicketFlow</h2>
      <p class="mb-4">© {{ 'now'|date('Y') }} TicketFlow. All rights reserved.</p>
      <div class="flex justify-center gap-6">
        <a href="#" class="hover:text-white">About</a>
        <a href="#" class="hover:text-white">Features</a>
        <a href="#" class="hover:text-white">Contact</a>
      </div>
    </div>
  </footer>
</div>
{% endblock %}
